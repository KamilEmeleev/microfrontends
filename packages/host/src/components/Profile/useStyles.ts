import { createStyles, makeStyles } from '@abdt/ornament';

const useStyles = makeStyles(() =>
    createStyles({
        avatar: {
            marginTop: 'auto',
            padding: 0,
        },
    })
);

export default useStyles;
